# 📘 Ejercitación JavaScript – Clase 17

Este repositorio contiene los ejercicios prácticos realizados durante la **Clase 17** del curso de **Introducción a JavaScript**, enfocados en el uso de métodos avanzados de arrays y strings, junto con la aplicación de algoritmos clásicos de búsqueda y ordenamiento.

## 🧠 Conocimientos aplicados

- Métodos avanzados de arrays:
  - `map()`
  - `filter()`
  - `find()`
  - `reduce()`
  - `forEach()`
  - `some()`
  - `findIndex()`
  - `sort()`
- Manipulación de strings:
  - `toUpperCase()`
  - `toLowerCase()`
  - `trim()`
  - `replace()`
  - `split()` y `join()`
- Trabajo con arrays de strings y objetos
- Encadenamiento de métodos
- Uso de funciones
- Validaciones y búsquedas dentro de arrays
- Algoritmos de búsqueda binaria
- Algoritmo de ordenamiento Bubble Sort
- Lógica aplicada y salida por consola

## 🛠️ Ejercicios desarrollados

- Manipulación completa de objetos literales y arrays
- Transformación y análisis de cadenas de texto
- Gestión y modificación de listas de productos
- Procesamiento y validación de listas de nombres
- Análisis y transformación de frases
- Gestión de listas de autos y florería
- Uso de métodos avanzados para búsquedas y reemplazos
- Implementación del algoritmo de búsqueda binaria
- Ordenamiento de arrays utilizando Bubble Sort
- Ejercicios integradores combinando lógica, arrays y strings

## 🎯 Objetivo

Profundizar en el uso de métodos avanzados de JavaScript para arrays y strings, mejorar la calidad del código y la lógica funcional, e introducir algoritmos clásicos que fortalecen el pensamiento computacional y la resolución de problemas.
